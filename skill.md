# CosyVoice 语音合成（astrbot_plugin_cosyvoice）

本插件让机器人以可配置音色朗读回复。LLM 应在合适的语义场景下调用下面的工具，而不是依赖固定关键词。

## 工具

### set_voice_mode(on: bool)
开启或关闭「当前会话（群）」的自动语音模式，按群记忆、重启不丢。
- 当用户表达「用语音回复 / 以后都用语音交流 / 念出来 / 用语音跟我说话」时，调用 `on=true`。
- 当用户表达「不用语音 / 用文字回复 / 别念了 / 关掉语音」时，调用 `on=false`。

### text_to_speech(text: str, voice: str = "")
把指定文本朗读出来（临时念一段特定内容）。
- 当用户明确要求「把这段话念出来 / 朗读以下内容 / 念一下这个」时使用。

## 使用约定
- 自动语音模式开启后，模型**正常用文字回复即可**，插件会自动把回复追加成语音，不需要为了"回复"而调用 `text_to_speech`。
- 仅在用户想「临时念一段特定内容」时才用 `text_to_speech`；想「长期用语音交流」请用 `set_voice_mode`。
- 用户也可用指令手动控制：`/tts_on`、`/tts_off` 切换当前群自动语音，`/tts <文本>` 临时朗读，`/tts_voice <音色名>` 切换音色。
- 若用户只是普通聊天、未要求语音，不要主动调用语音工具。
